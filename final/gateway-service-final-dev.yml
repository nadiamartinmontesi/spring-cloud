server:
  port: 8080

spring:
  cloud:
    gateway:
      default-filters:
        - name: LoggingFilter
      routes:
        - id: movieRoute
          uri: lb://movie-service-final
          predicates:
            - Path=/movies/**
        - id: catalogRoute
          uri: lb://catalog-service-final
          predicates:
            - Path=/catalogs/**
        - id: serieRoute
          uri: http://serie-service-final:8086/
          predicates:
            - Path=/series/**

info:
  app:
    name: ${spring.application.name}
    version: 1.0.0
    description: API Gateway Final Nadia Martin Montesi
